[flake8]
max-line-length = 120
exclude =
    .venv,
    .gemini,
    allure-report,
    allure-results,
    coverage-results,
    coverage.html,
    coverage-history.json,
    coverage-report.json,
    GEMINI.md,
    SPEC.md,
    StudyFiles